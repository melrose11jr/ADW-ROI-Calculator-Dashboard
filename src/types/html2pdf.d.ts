declare module 'html2pdf.js' {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const html2pdf: any;  // Or a more specific type if you can determine it
    export default html2pdf;
}